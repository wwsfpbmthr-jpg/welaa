import {sqliteTable,text,integer,real,index,primaryKey} from 'drizzle-orm/sqlite-core';
export const users=sqliteTable('users',{id:text('id').primaryKey(),name:text('name').notNull(),email:text('email').notNull(),phone:text('phone'),bio:text('bio'),createdAt:text('created_at').notNull()});
export const spaces=sqliteTable('spaces',{id:text('id').primaryKey(),owner:text('owner').notNull(),data:text('data').notNull(),published:integer('published').notNull().default(1)},t=>[index('spaces_owner_idx').on(t.owner)]);
export const spaceImages=sqliteTable('space_images',{id:text('id').primaryKey(),owner:text('owner').notNull(),spaceId:text('space_id'),key:text('key').notNull(),mime:text('mime').notNull()});
export const amenities=sqliteTable('amenities',{id:text('id').primaryKey(),name:text('name').notNull()});
export const availability=sqliteTable('availability',{spaceId:text('space_id').notNull(),date:text('date').notNull(),hour:integer('hour').notNull(),price:real('price').notNull(),instant:integer('instant').notNull().default(0)},t=>[primaryKey({columns:[t.spaceId,t.date,t.hour]})]);
export const bookings=sqliteTable('bookings',{id:text('id').primaryKey(),userId:text('user_id').notNull(),spaceId:text('space_id').notNull(),date:text('date').notNull(),start:integer('start').notNull(),end:integer('end').notNull(),guests:integer('guests').notNull(),subtotal:real('subtotal').notNull(),fee:real('fee').notNull(),total:real('total').notNull(),status:text('status').notNull(),createdAt:text('created_at').notNull()},t=>[index('bookings_user_idx').on(t.userId),index('bookings_space_idx').on(t.spaceId)]);
export const occupied=sqliteTable('occupied',{spaceId:text('space_id').notNull(),date:text('date').notNull(),hour:integer('hour').notNull(),bookingId:text('booking_id'),kind:text('kind').notNull()},t=>[primaryKey({columns:[t.spaceId,t.date,t.hour]})]);
export const payments=sqliteTable('payments',{id:text('id').primaryKey(),bookingId:text('booking_id').notNull(),provider:text('provider').notNull().default('mock'),amount:real('amount').notNull(),status:text('status').notNull()});
export const reviews=sqliteTable('reviews',{id:text('id').primaryKey(),userId:text('user_id').notNull(),spaceId:text('space_id').notNull(),bookingId:text('booking_id').notNull().unique(),rating:integer('rating').notNull(),comment:text('comment').notNull(),createdAt:text('created_at').notNull()});
export const messages=sqliteTable('messages',{id:text('id').primaryKey(),sender:text('sender').notNull(),recipient:text('recipient').notNull(),spaceId:text('space_id').notNull(),body:text('body').notNull(),createdAt:text('created_at').notNull()},t=>[index('messages_sender_idx').on(t.sender),index('messages_recipient_idx').on(t.recipient)]);
export const favorites=sqliteTable('favorites',{userId:text('user_id').notNull(),spaceId:text('space_id').notNull()},t=>[primaryKey({columns:[t.userId,t.spaceId]})]);
export const drafts=sqliteTable('drafts',{userId:text('user_id').primaryKey(),data:text('data').notNull()});
